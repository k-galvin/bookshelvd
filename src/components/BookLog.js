import { useState, useEffect } from 'react'
import { fetchAllBooks } from '../services/bookService'

const BookLog = () => {
  // Fetch all books in the log

  return <div>{/* Display all the books in the log */}</div>
}

export default BookLog
